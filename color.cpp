#include "Color.h"

Color::Color(void)
	:red(0.0), green(0.0), blue(0.0)
{
}

Color::Color(const float new_red, const float new_green, const float new_blue)
	:red(new_red), green(new_green), blue(new_blue)
{
}
